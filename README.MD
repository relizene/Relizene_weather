# My Weather App RElizene

Программа для получения прогноза погоды через API.

## Установка
```bash
git clone https://github.com/ваш-логин/my-weather-app.git
cd my-weather-app
pip install -r requirements.txt


Использование
python
from weather_app.core import get_weather
get_weather("Москва")